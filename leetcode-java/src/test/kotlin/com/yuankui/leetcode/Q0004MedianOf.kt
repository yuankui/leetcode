package com.yuankui.leetcode

import org.junit.jupiter.api.Test

class Q0004MedianOf {
    fun findMedianSortedArrays(nums1: IntArray, nums2: IntArray): Double {
        return 1.0
    }
    
    @Test
    fun test() {
        val a = 1.rangeTo(7)
        val b = 1.rangeTo(6).map { it * 2 }
        
        val c = (a + b).sorted()
        
        println(c)

        val (dd, ee) = listOf(1, 2, 3)
    }
}